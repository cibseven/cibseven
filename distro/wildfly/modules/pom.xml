<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.cibseven.bpm.wildfly</groupId>
    <artifactId>cibseven-wildfly</artifactId>
    <version>2.3.0-SNAPSHOT</version>
  </parent>

  <artifactId>cibseven-wildfly-modules</artifactId>
  <packaging>jar</packaging>
  <name>CIB seven - Wildfly Modules</name>

  <properties>
    <org.jboss.shrinkwrap.resolver.version>2.2.7</org.jboss.shrinkwrap.resolver.version>
  </properties>
  
  <dependencies>
    <dependency>
      <groupId>org.cibseven.bpm.wildfly</groupId>
      <artifactId>cibseven-wildfly-subsystem</artifactId>
      <version>${project.version}</version>
      <exclusions>
        <exclusion>
          <groupId>org.apache.commons</groupId>
          <artifactId>commons-email</artifactId>
        </exclusion>
        <exclusion>
          <groupId>org.springframework</groupId>
          <artifactId>spring-beans</artifactId>
        </exclusion>
        <exclusion>
          <groupId>commons-lang</groupId>
          <artifactId>commons-lang</artifactId>
        </exclusion>
      </exclusions>
    </dependency>

    <dependency>
      <groupId>org.cibseven.bpm.identity</groupId>
      <artifactId>cibseven-identity-ldap</artifactId>
    </dependency>
	
    <dependency>
      <groupId>org.cibseven.bpm.identity</groupId>
      <artifactId>cibseven-identity-scim</artifactId>
    </dependency>

    <dependency>
      <groupId>org.cibseven.bpm</groupId>
      <artifactId>cibseven-engine-plugin-spin</artifactId>
    </dependency>

    <dependency>
      <groupId>org.cibseven.spin</groupId>
      <artifactId>cibseven-spin-dataformat-json-jackson</artifactId>
    </dependency>

    <dependency>
      <groupId>org.cibseven.spin</groupId>
      <artifactId>cibseven-spin-dataformat-xml-dom-jakarta</artifactId>
    </dependency>

    <dependency>
      <groupId>org.cibseven.bpm</groupId>
      <artifactId>cibseven-engine-plugin-connect</artifactId>
    </dependency>

    <dependency>
      <groupId>org.cibseven.connect</groupId>
      <artifactId>cibseven-connect-http-client</artifactId>
    </dependency>

    <dependency>
      <groupId>org.cibseven.connect</groupId>
      <artifactId>cibseven-connect-soap-http-client</artifactId>
    </dependency>

    <!-- reactor-ordering only: ensures the ai-agent collector (which gathers the
         connector + its LangChain4j/ONNX/pdfbox transitive jars into
         target/dependency) builds before this module copies them into the fat
         ai-agent JBoss module. The *:* exclusion keeps those jars off this
         module's own classpath. -->
    <dependency>
      <groupId>org.cibseven.bpm.wildfly</groupId>
      <artifactId>cibseven-wildfly-ai-agent</artifactId>
      <version>${project.version}</version>
      <type>pom</type>
      <exclusions>
        <exclusion>
          <groupId>*</groupId>
          <artifactId>*</artifactId>
        </exclusion>
      </exclusions>
    </dependency>

    <!-- script engine dependencies -->
    <dependency>
      <groupId>org.apache.groovy</groupId>
      <artifactId>groovy</artifactId>
    </dependency>
    <dependency>
      <groupId>org.apache.groovy</groupId>
      <artifactId>groovy-jsr223</artifactId>
    </dependency>
    <dependency>
      <groupId>org.apache.groovy</groupId>
      <artifactId>groovy-json</artifactId>
    </dependency>
    <dependency>
      <groupId>org.apache.groovy</groupId>
      <artifactId>groovy-xml</artifactId>
    </dependency>
    <dependency>
      <groupId>org.apache.groovy</groupId>
      <artifactId>groovy-templates</artifactId>
    </dependency>
    <dependency>
      <groupId>org.apache.groovy</groupId>
      <artifactId>groovy-dateutil</artifactId>
    </dependency>
    <dependency>
      <groupId>org.apache.groovy</groupId>
      <artifactId>groovy-datetime</artifactId>
    </dependency>

    <dependency>
      <groupId>org.graalvm.js</groupId>
      <artifactId>js</artifactId>
    </dependency>
    <dependency>
      <groupId>org.graalvm.js</groupId>
      <artifactId>js-scriptengine</artifactId>
    </dependency>

    <!-- template engine dependencies -->
    <dependency>
      <groupId>org.freemarker</groupId>
      <artifactId>freemarker</artifactId>
    </dependency>

    <dependency>
      <groupId>org.cibseven.template-engines</groupId>
      <artifactId>cibseven-template-engines-freemarker</artifactId>
    </dependency>

    <dependency>
      <groupId>com.h2database</groupId>
      <artifactId>h2</artifactId>
    </dependency>

    <!-- Patched lz4-java replacing the 1.10.4 jar bundled in wildfly-dist (CVE-2026-59949).
         WildFly's system module is excluded from the distro.
         copy-dependencies step below already maps <groupId path>/<version>/<jar> to
         <groupId path>/main/<jar>, which is exactly the layout the module.xml expects.
         Drop this once WildFly ships lz4-java >= 1.11.1. -->
    <dependency>
      <groupId>at.yawk.lz4</groupId>
      <artifactId>lz4-java</artifactId>
      <version>${version.lz4-java}</version>
    </dependency>

  </dependencies>

  <build>
    <finalName>${project.artifactId}</finalName>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-antrun-plugin</artifactId>
        <executions>
          <execution>
            <id>copy-dependencies</id>
            <phase>prepare-package</phase>
            <goals>
              <goal>run</goal>
            </goals>
            <configuration>
              <target>
                <dependencyfilesets scopes="compile,runtime" />

                <mkdir dir="target/modules" />

                <!-- copy all files -->
                <copy todir="target/modules" flatten="false">
                  <fileset refid="maven.project.dependencies" />
                  <regexpmapper from="^(.*)/([^/]+)/([^/]*)$$" to="\1/main/\3" handledirsep="yes" />
                </copy>

                <delete dir="target/modules/org/cibseven/bpm" />

                <!-- copy all files again, but this only matches fox files and this treatment makes is maven 2/3 compatible => timestamped snapshot -->
                <copy todir="target/modules" flatten="false">
                  <fileset refid="maven.project.dependencies" />
                  <mapper>
                    <chainedmapper>
                      <!-- if any link in the chain doesn't match, the file is dropped (i.e. all non org.cibseven.bpm.*)-->
                      <regexpmapper from="^(.*)/([^/]+)/([^/]*)$$" to="\1/main/\3" handledirsep="yes" />
                      <regexpmapper from="^(org.cibseven.bpm.*)/((cibseven-[A-Za-z-]*)[0-9\.]*).*.jar$$" to="\1/\3${project.version}.jar" handledirsep="yes" />
                    </chainedmapper>
                  </mapper>
                </copy>

                <delete dir="target/modules/org/cibseven/spin" />
                <copy todir="target/modules" flatten="false">
                  <fileset refid="maven.project.dependencies" />
                  <mapper>
                    <chainedmapper>
                      <regexpmapper from="^(.*)/([^/]+)/([^/]*)$$" to="\1/main/\3" handledirsep="yes" />
                      <regexpmapper from="^(org.cibseven.spin.*)/((cibseven-[A-Za-z-]*)[0-9\.]*).*.jar$$" to="\1/\3${project.version}.jar" handledirsep="yes" />
                    </chainedmapper>
                  </mapper>
                </copy>

                <delete dir="target/modules/org/cibseven/connect" />
                <copy todir="target/modules" flatten="false">
                  <fileset refid="maven.project.dependencies" />
                  <mapper>
                    <chainedmapper>
                      <regexpmapper from="^(.*)/([^/]+)/([^/]*)$$" to="\1/main/\3" handledirsep="yes" />
                      <regexpmapper from="^(org.cibseven.connect.*)/((cibseven-[A-Za-z-]*)[0-9\.]*).*.jar$$" to="\1/\3${project.version}.jar" handledirsep="yes" />
                    </chainedmapper>
                  </mapper>
                </copy>

                <!-- do not include connectors-all dependency, because
                  we are using the individual connect modules on Wildfly -->
                <delete dir="target/modules/org/cibseven/connect/cibseven-connect-connectors-all" />

                <!-- ai-agent fat module (CIB7-1445): copy the connector + all its
                     LangChain4j/ONNX/pdfbox/pgvector transitive jars (gathered by the
                     cibseven-wildfly-ai-agent collector) into a single module dir.
                     Done after the org/cibseven/connect delete+copy above so it is
                     not wiped. The matching module.xml (with a generated
                     resource-root list) is added by the src/main/modules copy + the
                     resource-root generation below. -->
                <mkdir dir="target/modules/org/cibseven/connect/cibseven-connect-ai-agent/main" />
                <copy todir="target/modules/org/cibseven/connect/cibseven-connect-ai-agent/main" flatten="true">
                  <fileset dir="../ai-agent/target/dependency">
                    <include name="*.jar" />
                  </fileset>
                </copy>

                <delete dir="target/modules/org/cibseven/commons" />
                <copy todir="target/modules" flatten="false">
                  <fileset refid="maven.project.dependencies" />
                  <mapper>
                    <chainedmapper>
                      <regexpmapper from="^(.*)/([^/]+)/([^/]*)$$" to="\1/main/\3" handledirsep="yes" />
                      <mapper>
                        <regexpmapper from="^(org.cibseven.commons.*)\/((cibseven-commons-(utils|logging)-[A-Za-z-]*)[0-9\.]*).*.jar$$" to="\1/\3${project.version}.jar" handledirsep="yes" />
                        <regexpmapper from="^(org.cibseven.commons.*)\/((cibseven-commons-typed-values-)[0-9\.]*).*.jar$$" to="\1/\3${project.version}.jar" handledirsep="yes" />
                      </mapper>
                    </chainedmapper>
                  </mapper>
                </copy>

                <delete dir="target/modules/org/cibseven/template-engines" />
                <copy todir="target/modules" flatten="false">
                  <fileset refid="maven.project.dependencies" />
                  <mapper>
                    <chainedmapper>
                      <regexpmapper from="^(.*)/([^/]+)/([^/]*)$$" to="\1/main/\3" handledirsep="yes" />
                      <regexpmapper from="^(org.cibseven.template-engines.*)/((cibseven-[A-Za-z-]*)[0-9\.]*).*.jar$$" to="\1/\3${project.version}.jar" handledirsep="yes" />
                    </chainedmapper>
                  </mapper>
                </copy>

                <delete dir="target/modules/org/camunda/feel" />
                <copy todir="target/modules" flatten="false">
                  <fileset refid="maven.project.dependencies" />
                  <mapper>
                    <chainedmapper>
                      <regexpmapper from="^(.*)/([^/]+)/([^/]*)$$" to="\1/main/\3" handledirsep="yes" />
                      <regexpmapper from="^(org.camunda.feel.feel-engine.*)/(([A-Za-z-]*)[0-9\.]*).*.jar$$" to="\1/\3${version.feel-scala}-scala-shaded.jar" handledirsep="yes" />
                    </chainedmapper>
                  </mapper>
                </copy>

                <!-- copy jackson dependencies to slot -->
                <delete dir="target/modules/com/fasterxml/jackson" />
                <copy todir="target/modules" flatten="false">
                  <fileset refid="maven.project.dependencies" />
                  <mapper>
                    <chainedmapper>
                      <regexpmapper from="^(.*)/([^/]+)/([^/]*)$$" to="\1/${version.jackson}/\3" handledirsep="yes" />
                      <regexpmapper from="^(com.fasterxml.jackson.*)\/((jackson.*-)[0-9\.]*).*.jar$$" to="\1/\3${version.jackson}.jar" handledirsep="yes" />
                    </chainedmapper>
                  </mapper>
                </copy>

                <!-- httpcore5-h2 along with httpcore5 -->
                <copy todir="target/modules" flatten="false">
                  <fileset dir="target/modules">
                    <include name="org/apache/httpcomponents/core5/httpcore5-h2/main/*.jar"/>
                  </fileset>
                  <chainedmapper>
                    <regexpmapper 
                      from="^org/apache/httpcomponents/core5/httpcore5-h2/main/(.*)$$" 
                      to="org/apache/httpcomponents/core5/httpcore5/main/\1" 
                      handledirsep="yes" />
                  </chainedmapper>
                </copy>
                <delete dir="target/modules/org/apache/httpcomponents/core5/httpcore5-h2" />

                <!-- move caffeine into its own slot, so that we do not shadow the
                  caffeine module WildFly provides for Infinispan -->
                <delete dir="target/modules/com/github/ben-manes/caffeine" />
                <copy todir="target/modules" flatten="false">
                  <fileset refid="maven.project.dependencies" />
                  <mapper>
                    <chainedmapper>
                      <regexpmapper from="^(com.github.ben-manes.caffeine)/caffeine/[^/]+/(caffeine-.*\.jar)$$" to="\1/cibseven/\2" handledirsep="yes" />
                    </chainedmapper>
                  </mapper>
                </copy>

                <!-- copy groovy dependencies to single dir -->
                <delete dir="target/modules/org/apache/groovy" />
                <copy todir="target/modules" flatten="false">
                  <fileset refid="maven.project.dependencies" />
                  <mapper>
                    <chainedmapper>
                      <regexpmapper from="^(.*)/([^/]+)/([^/]+)/([^/]*)$$" to="\1/groovy-all/main/\4" handledirsep="yes" />
                      <regexpmapper from="^(org.apache.groovy.*/.*.jar)$$" to="\1" handledirsep="yes" />
                    </chainedmapper>
                  </mapper>
                </copy>

                <!-- module descriptors, with @version.*@ tokens already substituted by
                     maven-resources-plugin (process-resources phase), must land here:
                     after path replacements above, before the jackson relocation
                     and the ai-agent resource-root generation below. -->
                <copy todir="target/modules" flatten="false">
                  <fileset dir="target/modules-filtered" />
                </copy>

                <!-- relocate jackson module.xml files from '@version.jackson@' placeholder to version-named slot -->
                <move todir="target/modules" flatten="false">
                  <fileset dir="target/modules">
                    <include name="com/fasterxml/jackson/**/@version.jackson@/module.xml" />
                  </fileset>
                  <regexpmapper from="^(com/fasterxml/jackson.*)/@version.jackson@/(module.xml)$$"
                                to="\1/${version.jackson}/\2"
                                handledirsep="yes" />
                </move>
                <delete dir="target/modules/com/fasterxml/jackson/core/jackson-annotations/@version.jackson@" />
                <delete dir="target/modules/com/fasterxml/jackson/core/jackson-core/@version.jackson@" />
                <delete dir="target/modules/com/fasterxml/jackson/core/jackson-databind/@version.jackson@" />

                <!-- drop jars that no module.xml claims: the copies above (maven.project.dependencies)
                 mirror the whole resolved dependency tree, src/main/modules is the actual module list -->
                <delete includeemptydirs="true">
                  <fileset dir="target/modules">
                    <include name="**/*.jar" />
                    <present present="srconly" targetdir="target/modules">
                      <regexpmapper from="^(.*)/[^/]*\.jar$$" to="\1/module.xml" handledirsep="yes" />
                    </present>
                  </fileset>
                </delete>

                <!-- sweep the directories left empty by the delete above -->
                <delete includeemptydirs="true">
                  <fileset dir="target/modules">
                    <type type="dir" />
                  </fileset>
                </delete>

                <!-- ai-agent fat module: generate one <resource-root> per collected
                     jar and inject it into the @resource-roots@ token of the shipped
                     module.xml template (so the list never drifts as the connector's
                     dependency set changes). -->
                <pathconvert property="ai.agent.resource.roots" pathsep="&#10;">
                  <fileset dir="target/modules/org/cibseven/connect/cibseven-connect-ai-agent/main">
                    <include name="*.jar" />
                  </fileset>
                  <chainedmapper>
                    <flattenmapper />
                    <globmapper from="*" to="    &lt;resource-root path=&quot;*&quot;/&gt;" />
                  </chainedmapper>
                </pathconvert>
                <replace file="target/modules/org/cibseven/connect/cibseven-connect-ai-agent/main/module.xml"
                         token="@resource-roots@" value="${ai.agent.resource.roots}" />

              </target>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <executions>
          <execution>
            <id>copy</id>
            <phase>prepare-package</phase>
            <goals>
              <goal>copy</goal>
            </goals>
            <configuration>
              <artifactItems>
                <artifactItem>
                  <groupId>joda-time</groupId>
                  <artifactId>joda-time</artifactId>
                  <outputDirectory>${project.build.directory}/modules/org/joda/time/2.1/</outputDirectory>
                </artifactItem>
              </artifactItems>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <!-- substitute the @version.*@ tokens in the module descriptors into a staging
           dir. Bound to process-resources so it always runs before the antrun target,
           independently of plugin declaration order. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-resources-plugin</artifactId>
        <executions>
          <execution>
            <id>filter-modules</id>
            <phase>process-resources</phase>
            <goals>
              <goal>copy-resources</goal>
            </goals>
            <configuration>
              <outputDirectory>${project.build.directory}/modules-filtered</outputDirectory>
              <resources>
                <resource>
                  <directory>src/main/modules</directory>
                  <filtering>true</filtering>
                </resource>
              </resources>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <configuration>
          <classesDirectory>${project.build.directory}/modules/</classesDirectory>
        </configuration>
      </plugin>
    </plugins>
  </build>
  <description>
    Description of CIB seven - Wildfly Modules
  </description>

  <licenses>
    <license>
      <name>The Apache Software License, Version 2.0</name>
      <url>http://www.apache.org/licenses/LICENSE-2.0.txt</url>
    </license>
  </licenses>

  <organization>
    <name>CIB seven</name>
    <url>https://cibseven.org</url>
  </organization>

  <url>https://cibseven.org</url>

  <developers>
    <developer>
      <id>CIB seven</id>
      <name>CIB seven Community</name>
      <organization>CIB seven</organization>
      <organizationUrl>https://cibseven.org</organizationUrl>
    </developer>
  </developers>

  <scm>
    <url>https://github.com/cibseven/cibseven</url>
    <connection>scm:git:git@github.com:cibseven/cibseven.git</connection>
    <developerConnection>scm:git:git@github.com:cibseven/cibseven.git</developerConnection>
    <tag>HEAD</tag>
  </scm>

  <issueManagement>
    <system>GitHub Issues</system>
    <url>https://github.com/cibseven/cibseven/issues</url>
  </issueManagement>

</project>
