<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.cibseven.bpm.run</groupId>
    <artifactId>cibseven-bpm-run-root</artifactId>
    <version>2.3.0-SNAPSHOT</version>
    <relativePath>..</relativePath>
  </parent>

  <artifactId>cibseven-bpm-run-qa</artifactId>
  <name>CIB seven - Run - QA</name>
  <packaging>pom</packaging>

  <properties>
    <!-- default os -->
    <os.type>linux64</os.type>

    <version.jersey-json>1.15</version.jersey-json>
    <version.jersey-apache-client>1.15</version.jersey-apache-client>
    <version.httpcomponents>5.3.1</version.httpcomponents>
    <version.junit4>4.13.1</version.junit4>
  </properties>

  <dependencyManagement>
    <dependencies>
      <!-- Must stay FIRST: among imported BOMs the earliest declaration wins. distro/run/pom.xml
           imports spring-boot-dependencies, which manages the JUnit artifacts itself, so without
           this import the engine and platform are pinned by Spring Boot rather than by
           ${version.junit5}. A provider/platform mismatch makes failsafe report "No tests to run"
           instead of failing, so the suite goes silently green. -->
      <dependency>
        <groupId>org.junit</groupId>
        <artifactId>junit-bom</artifactId>
        <version>${version.junit5}</version>
        <scope>import</scope>
        <type>pom</type>
      </dependency>

      <dependency>
        <groupId>org.apache.httpcomponents.client5</groupId>
        <artifactId>httpclient5-parent</artifactId>
        <version>${version.httpcomponents}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>

      <dependency>
        <groupId>org.seleniumhq.selenium</groupId>
        <artifactId>selenium-java</artifactId>
        <version>${version.selenium}</version>
      </dependency>

    </dependencies>
  </dependencyManagement>

  <profiles>
    <profile>
      <id>distro</id>
      <activation>
        <activeByDefault>true</activeByDefault>
      </activation>
      <modules>
        <module>example-plugin</module>
        <module>integration-tests</module>
        <module>runtime</module>
      </modules>
    </profile>
  </profiles>

  <description>
    Description of CIB seven - Run - QA
  </description>

  <licenses>
    <license>
      <name>The Apache Software License, Version 2.0</name>
      <url>http://www.apache.org/licenses/LICENSE-2.0.txt</url>
    </license>
  </licenses>

  <organization>
    <name>CIB seven</name>
    <url>https://cibseven.org</url>
  </organization>

  <url>https://cibseven.org</url>

  <developers>
    <developer>
      <id>CIB seven</id>
      <name>CIB seven Community</name>
      <organization>CIB seven</organization>
      <organizationUrl>https://cibseven.org</organizationUrl>
    </developer>
  </developers>

  <scm>
    <url>https://github.com/cibseven/cibseven</url>
    <connection>scm:git:git@github.com:cibseven/cibseven.git</connection>
    <developerConnection>scm:git:git@github.com:cibseven/cibseven.git</developerConnection>
    <tag>HEAD</tag>
  </scm>

  <issueManagement>
    <system>GitHub Issues</system>
    <url>https://github.com/cibseven/cibseven/issues</url>
  </issueManagement>

</project>
