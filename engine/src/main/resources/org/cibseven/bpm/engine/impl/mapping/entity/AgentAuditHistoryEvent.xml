<?xml version="1.0" encoding="UTF-8" ?>
<!--

    Copyright CIB software GmbH and/or licensed to CIB software GmbH
    under one or more contributor license agreements. See the NOTICE file
    distributed with this work for additional information regarding copyright
    ownership. CIB software licenses this file to you under the Apache License,
    Version 2.0; you may not use this file except in compliance with the License.
    You may obtain a copy of the License at

        http://www.apache.org/licenses/LICENSE-2.0

    Unless required by applicable law or agreed to in writing, software
    distributed under the License is distributed on an "AS IS" BASIS,
    WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
    See the License for the specific language governing permissions and
    limitations under the License.

-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.cibseven.bpm.engine.impl.history.event.AgentAuditHistoryEventEntity">

  <!-- AGENT AUDIT INSERT -->

  <insert id="insertAgentAuditHistoryEvent" parameterType="org.cibseven.bpm.engine.impl.history.event.AgentAuditHistoryEventEntity">
      insert into ${prefix}ACT_HI_AGENT_AUDIT (
        ID_,
        ROOT_PROC_INST_ID_,
        PROC_DEF_KEY_,
        PROC_DEF_ID_,
        PROC_INST_ID_,
        EXECUTION_ID_,
        ACT_ID_,
        TENANT_ID_,
        TIMESTAMP_,
        SCHEMA_VERSION_,
        AUDIT_TYPE_,
        RUN_ID_,
        EVENT_SEQ_,
        PROVIDER_,
        MODEL_,
        RESPONSE_ID_,
        ENDPOINT_,
        USER_ID_,
        GROUP_IDS_,
        FINISH_REASON_,
        DURATION_,
        ERROR_CLASS_,
        MODEL_PARAMS_,
        PAYLOAD_BYTEARRAY_ID_,
        REMOVAL_TIME_
      ) values (
        #{id, jdbcType=VARCHAR},
        #{rootProcessInstanceId, jdbcType=VARCHAR},
        #{processDefinitionKey, jdbcType=VARCHAR},
        #{processDefinitionId, jdbcType=VARCHAR},
        #{processInstanceId, jdbcType=VARCHAR},
        #{executionId, jdbcType=VARCHAR},
        #{activityId, jdbcType=VARCHAR},
        #{tenantId, jdbcType=VARCHAR},
        #{timestamp, jdbcType=TIMESTAMP},
        #{schemaVersion, jdbcType=INTEGER},
        #{auditType, jdbcType=VARCHAR},
        #{runId, jdbcType=VARCHAR},
        #{eventSeq, jdbcType=INTEGER},
        #{provider, jdbcType=VARCHAR},
        #{model, jdbcType=VARCHAR},
        #{responseId, jdbcType=VARCHAR},
        #{endpoint, jdbcType=VARCHAR},
        #{userId, jdbcType=VARCHAR},
        #{groupIds, jdbcType=VARCHAR},
        #{finishReason, jdbcType=VARCHAR},
        #{durationMs, jdbcType=BIGINT},
        #{errorClass, jdbcType=VARCHAR},
        #{modelParams, jdbcType=VARCHAR},
        #{payloadByteArrayId, jdbcType=VARCHAR},
        #{removalTime, jdbcType=TIMESTAMP}
      )
  </insert>

  <!-- AGENT AUDIT DELETE -->

  <delete id="deleteAgentAuditHistoryEvent" parameterType="org.cibseven.bpm.engine.impl.history.event.AgentAuditHistoryEventEntity">
    delete from ${prefix}ACT_HI_AGENT_AUDIT where ID_ = #{id}
  </delete>

  <delete id="deleteAgentAuditByProcessInstanceIds">
    delete from ${prefix}ACT_HI_AGENT_AUDIT
    where
      <bind name="listOfIds" value="list"/>
      <bind name="fieldName" value="'PROC_INST_ID_'"/>
      <include refid="org.cibseven.bpm.engine.impl.persistence.entity.Commons.applyInForPaginatedCollection"/>
  </delete>

  <!-- Must run before the rows themselves are deleted, otherwise the payloads are orphaned. -->
  <delete id="deleteAgentAuditByteArraysByProcessInstanceIds">
    delete from ${prefix}ACT_GE_BYTEARRAY
    where ID_ in (
      select DISTINCT PAYLOAD_BYTEARRAY_ID_
      from ${prefix}ACT_HI_AGENT_AUDIT
      where PAYLOAD_BYTEARRAY_ID_ is not NULL
      and
        <bind name="listOfIds" value="list"/>
        <bind name="fieldName" value="'PROC_INST_ID_'"/>
        <include refid="org.cibseven.bpm.engine.impl.persistence.entity.Commons.applyInForPaginatedCollection"/>
    )
  </delete>

  <delete id="deleteAgentAuditByteArraysByProcessInstanceIds_mysql">
    delete B from ${prefix}ACT_GE_BYTEARRAY B
    inner join ${prefix}ACT_HI_AGENT_AUDIT A
      on B.ID_ = A.PAYLOAD_BYTEARRAY_ID_
    where A.PAYLOAD_BYTEARRAY_ID_ is not NULL
    and
      <bind name="listOfIds" value="list"/>
      <bind name="fieldName" value="'A.PROC_INST_ID_'"/>
      <include refid="org.cibseven.bpm.engine.impl.persistence.entity.Commons.applyInForPaginatedCollection"/>
  </delete>

  <!-- AGENT AUDIT RESULTMAP -->

  <resultMap id="agentAuditHistoryEventResultMap" type="org.cibseven.bpm.engine.impl.history.event.AgentAuditHistoryEventEntity">
    <id property="id" column="ID_" jdbcType="VARCHAR" />
    <result property="rootProcessInstanceId" column="ROOT_PROC_INST_ID_" jdbcType="VARCHAR" />
    <result property="processDefinitionKey" column="PROC_DEF_KEY_" jdbcType="VARCHAR" />
    <result property="processDefinitionId" column="PROC_DEF_ID_" jdbcType="VARCHAR" />
    <result property="processInstanceId" column="PROC_INST_ID_" jdbcType="VARCHAR" />
    <result property="executionId" column="EXECUTION_ID_" jdbcType="VARCHAR" />
    <result property="activityId" column="ACT_ID_" jdbcType="VARCHAR" />
    <result property="tenantId" column="TENANT_ID_" jdbcType="VARCHAR" />
    <result property="timestamp" column="TIMESTAMP_" jdbcType="TIMESTAMP" />
    <result property="schemaVersion" column="SCHEMA_VERSION_" jdbcType="INTEGER" />
    <result property="auditType" column="AUDIT_TYPE_" jdbcType="VARCHAR" />
    <result property="runId" column="RUN_ID_" jdbcType="VARCHAR" />
    <result property="eventSeq" column="EVENT_SEQ_" jdbcType="INTEGER" />
    <result property="provider" column="PROVIDER_" jdbcType="VARCHAR" />
    <result property="model" column="MODEL_" jdbcType="VARCHAR" />
    <result property="responseId" column="RESPONSE_ID_" jdbcType="VARCHAR" />
    <result property="endpoint" column="ENDPOINT_" jdbcType="VARCHAR" />
    <result property="userId" column="USER_ID_" jdbcType="VARCHAR" />
    <result property="groupIds" column="GROUP_IDS_" jdbcType="VARCHAR" />
    <result property="finishReason" column="FINISH_REASON_" jdbcType="VARCHAR" />
    <result property="durationMs" column="DURATION_" jdbcType="BIGINT" />
    <result property="errorClass" column="ERROR_CLASS_" jdbcType="VARCHAR" />
    <result property="modelParams" column="MODEL_PARAMS_" jdbcType="VARCHAR" />
    <result property="payloadByteArrayId" column="PAYLOAD_BYTEARRAY_ID_" jdbcType="VARCHAR" />
    <result property="removalTime" column="REMOVAL_TIME_" jdbcType="TIMESTAMP" />
  </resultMap>

  <!-- AGENT AUDIT SELECT -->

  <select id="selectAgentAuditHistoryEvent" parameterType="string" resultMap="agentAuditHistoryEventResultMap">
    select * from ${prefix}ACT_HI_AGENT_AUDIT where ID_ = #{id}
  </select>

  <select id="selectAgentAuditByRunId" parameterType="string" resultMap="agentAuditHistoryEventResultMap">
    select * from ${prefix}ACT_HI_AGENT_AUDIT
    where RUN_ID_ = #{runId}
    order by EVENT_SEQ_ asc
  </select>

  <select id="selectAgentAuditByProcessInstanceId" parameterType="string" resultMap="agentAuditHistoryEventResultMap">
    select * from ${prefix}ACT_HI_AGENT_AUDIT
    where PROC_INST_ID_ = #{processInstanceId}
    order by TIMESTAMP_ asc, EVENT_SEQ_ asc
  </select>

  <!-- AGENT AUDIT UPDATE REMOVAL TIME -->

  <update id="updateAgentAuditByRootProcessInstanceId"
          parameterType="java.util.Map">
    update
    <if test="maxResults != null">
      ${limitBeforeWithoutOffset}
    </if>
    ${prefix}ACT_HI_AGENT_AUDIT set
    REMOVAL_TIME_ = #{removalTime, jdbcType=TIMESTAMP}

    where ROOT_PROC_INST_ID_ = #{rootProcessInstanceId, jdbcType=VARCHAR}
    <if test="maxResults != null">
      and (REMOVAL_TIME_ is null or REMOVAL_TIME_ != #{removalTime, jdbcType=TIMESTAMP})
      ${limitAfterWithoutOffset}
    </if>
  </update>

  <update id="updateAgentAuditByRootProcessInstanceId_postgres"
          parameterType="java.util.Map">
    update ${prefix}ACT_HI_AGENT_AUDIT set
    REMOVAL_TIME_ = #{removalTime, jdbcType=TIMESTAMP}
    <if test="maxResults != null">
      ${limitBeforeInUpdate} ${prefix}ACT_HI_AGENT_AUDIT
    </if>

    where ROOT_PROC_INST_ID_ = #{rootProcessInstanceId, jdbcType=VARCHAR}
    <if test="maxResults != null">
      and (REMOVAL_TIME_ is null or REMOVAL_TIME_ != #{removalTime, jdbcType=TIMESTAMP})
      ${limitAfterInUpdate}
    </if>
  </update>

  <update id="updateAgentAuditByRootProcessInstanceId_mssql"
          parameterType="java.util.Map">
    update
    <if test="maxResults != null">
      ${limitBeforeWithoutOffset}
    </if>
    RES set
    RES.REMOVAL_TIME_ = #{removalTime, jdbcType=TIMESTAMP}
    FROM ${prefix}ACT_HI_AGENT_AUDIT RES WITH (FORCESEEK)
    where RES.ROOT_PROC_INST_ID_ = #{rootProcessInstanceId, jdbcType=VARCHAR}
    <if test="maxResults != null">
      and (REMOVAL_TIME_ is null or REMOVAL_TIME_ != #{removalTime, jdbcType=TIMESTAMP})
      ${limitAfterWithoutOffset}
    </if>
  </update>

  <update id="updateAgentAuditByRootProcessInstanceId_mysql"
          parameterType="java.util.Map">
    update ${prefix}ACT_HI_AGENT_AUDIT
    <choose>
      <when test="maxResults != null">
        ${limitBeforeInUpdate} ${prefix}ACT_HI_AGENT_AUDIT
      </when>
      <otherwise>
        set REMOVAL_TIME_ = #{removalTime, jdbcType=TIMESTAMP},
            TIMESTAMP_ = TIMESTAMP_
      </otherwise>
    </choose>

    where ROOT_PROC_INST_ID_ = #{rootProcessInstanceId, jdbcType=VARCHAR}
    <if test="maxResults != null">
      and (REMOVAL_TIME_ is null or REMOVAL_TIME_ != #{removalTime, jdbcType=TIMESTAMP})
      ${limitAfterInUpdate}
      set REMOVAL_TIME_ = #{removalTime, jdbcType=TIMESTAMP},
          TIMESTAMP_ = TIMESTAMP_
    </if>
  </update>

  <update id="updateAgentAuditByProcessInstanceId"
          parameterType="java.util.Map">
    update
    <if test="maxResults != null">
      ${limitBeforeWithoutOffset}
    </if>
    ${prefix}ACT_HI_AGENT_AUDIT
      set REMOVAL_TIME_ = #{removalTime, jdbcType=TIMESTAMP}
      where PROC_INST_ID_ = #{processInstanceId, jdbcType=VARCHAR}
      <if test="maxResults != null">
        and (REMOVAL_TIME_ is null or REMOVAL_TIME_ != #{removalTime, jdbcType=TIMESTAMP})
        ${limitAfterWithoutOffset}
      </if>
  </update>

  <update id="updateAgentAuditByProcessInstanceId_postgres"
          parameterType="java.util.Map">
    update ${prefix}ACT_HI_AGENT_AUDIT set
    REMOVAL_TIME_ = #{removalTime, jdbcType=TIMESTAMP}
    <if test="maxResults != null">
      ${limitBeforeInUpdate} ${prefix}ACT_HI_AGENT_AUDIT
    </if>

    where PROC_INST_ID_ = #{processInstanceId, jdbcType=VARCHAR}
    <if test="maxResults != null">
      and (REMOVAL_TIME_ is null or REMOVAL_TIME_ != #{removalTime, jdbcType=TIMESTAMP})
      ${limitAfterInUpdate}
    </if>
  </update>

  <update id="updateAgentAuditByProcessInstanceId_mssql"
          parameterType="java.util.Map">
    update
    <if test="maxResults != null">
      ${limitBeforeWithoutOffset}
    </if>
    RES set
    RES.REMOVAL_TIME_ = #{removalTime, jdbcType=TIMESTAMP}
    FROM ${prefix}ACT_HI_AGENT_AUDIT RES WITH (FORCESEEK)
    where RES.PROC_INST_ID_ = #{processInstanceId, jdbcType=VARCHAR}
    <if test="maxResults != null">
      and (REMOVAL_TIME_ is null or REMOVAL_TIME_ != #{removalTime, jdbcType=TIMESTAMP})
      ${limitAfterWithoutOffset}
    </if>
  </update>

  <update id="updateAgentAuditByProcessInstanceId_mysql"
          parameterType="java.util.Map">
    update ${prefix}ACT_HI_AGENT_AUDIT
    <choose>
      <when test="maxResults != null">
        ${limitBeforeInUpdate} ${prefix}ACT_HI_AGENT_AUDIT
      </when>
      <otherwise>
        set REMOVAL_TIME_ = #{removalTime, jdbcType=TIMESTAMP},
            TIMESTAMP_ = TIMESTAMP_
      </otherwise>
    </choose>

    where PROC_INST_ID_ = #{processInstanceId, jdbcType=VARCHAR}
    <if test="maxResults != null">
      and (REMOVAL_TIME_ is null or REMOVAL_TIME_ != #{removalTime, jdbcType=TIMESTAMP})
      ${limitAfterInUpdate}
      set REMOVAL_TIME_ = #{removalTime, jdbcType=TIMESTAMP},
          TIMESTAMP_ = TIMESTAMP_
    </if>
  </update>

  <!-- AGENT AUDIT DELETE BY REMOVAL TIME -->

  <!-- Restricts a parallel cleanup job to its slice of the hour; renders empty when a single
       job owns all 60 minutes, so no redundant function call lands on the indexed column. -->
  <sql id="andWhereMinuteInDateBetweenSql">
    <if test="parameter.minuteFrom != null and parameter.minuteTo != null">
      AND ${datepart1}<include refid="org.cibseven.bpm.engine.impl.persistence.entity.Commons.periodUnitFunction"/>${datepart2}${date}${datepart3}
      between #{parameter.minuteFrom, jdbcType=INTEGER} and #{parameter.minuteTo, jdbcType=INTEGER}
    </if>
  </sql>

  <sql id="andWhereMinuteInDateBetweenSql_oracle">
    <if test="parameter.minuteFrom != null and parameter.minuteTo != null">
      AND ${datepart1}${date}${datepart2}<include refid="org.cibseven.bpm.engine.impl.persistence.entity.Commons.periodUnitFunction"/>${datepart3}
      between #{parameter.minuteFrom, jdbcType=INTEGER} and #{parameter.minuteTo, jdbcType=INTEGER}
    </if>
  </sql>

  <delete id="deleteAgentAuditByRemovalTime"
          parameterType="org.cibseven.bpm.engine.impl.db.ListQueryParameterObject">
    <bind name="date" value="'REMOVAL_TIME_'"/>
    <bind name="reportPeriodUnitName" value="'MINUTE'"/>
    delete ${limitBeforeWithoutOffset} from ${prefix}ACT_HI_AGENT_AUDIT
    where REMOVAL_TIME_ &lt;= #{parameter.removalTime}
    <include refid="andWhereMinuteInDateBetweenSql"/>
    ${limitAfterWithoutOffset}
  </delete>

  <delete id="deleteAgentAuditByRemovalTime_oracle"
          parameterType="org.cibseven.bpm.engine.impl.db.ListQueryParameterObject">
    <bind name="date" value="'REMOVAL_TIME_'"/>
    <bind name="reportPeriodUnitName" value="'MINUTE'"/>
    delete ${limitBeforeWithoutOffset} from ${prefix}ACT_HI_AGENT_AUDIT
    where REMOVAL_TIME_ &lt;= #{parameter.removalTime}
    <include refid="andWhereMinuteInDateBetweenSql_oracle"/>
    ${limitAfterWithoutOffset}
  </delete>

  <delete id="deleteAgentAuditByRemovalTime_postgres_or_db2"
          parameterType="org.cibseven.bpm.engine.impl.db.ListQueryParameterObject">
    <bind name="date" value="'REMOVAL_TIME_'"/>
    <bind name="reportPeriodUnitName" value="'MINUTE'"/>
    delete ${limitBeforeWithoutOffset} from ${prefix}ACT_HI_AGENT_AUDIT
    where ID_ IN
      (SELECT ID_
       FROM ${prefix}ACT_HI_AGENT_AUDIT
       WHERE REMOVAL_TIME_ &lt;= #{parameter.removalTime} <include refid="andWhereMinuteInDateBetweenSql"/>
       ${limitAfterWithoutOffset})
  </delete>

</mapper>
